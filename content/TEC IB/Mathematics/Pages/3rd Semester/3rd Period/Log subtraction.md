The log subtraction is a property of the [[Logarithm|logarithms]]. When two values inside a log are being divided, it is the same as subtracting both of those number's logs. This is seen below.

$$
\log_{a}(\frac{P}{Q})=\log_{a}(P)-\log_{a}(Q)
$$
