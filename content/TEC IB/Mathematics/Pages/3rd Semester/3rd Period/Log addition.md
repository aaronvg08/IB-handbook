The log addition is a property of the [[Logarithm|logarithms]]. When two values inside a log are being multiplied, it is the same as adding both of those number's logs. This is seen below.

$$
\log_{a}(PQ)=\log_{a}(P)+\log_{a}(Q)
$$
