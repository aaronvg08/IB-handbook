Sine is a [[Trigonometric ratios|trigonometric ratio]] that connects the hypotenuse and opposite side of a triangle relative to a specific angle. The ratio is stated as follows.

$$
\sin \theta=\frac{\text{Opposite}}{\text{Hypotenuse}}
$$

# Function

A sine can also be represented in a [[Function|function]], which makes it a trigonometric function. It has to do with its representation within the unit circle, and it acts like a wave oscillating up and down. A sine function follows the following format.

$$
f(x)=A\cdot \sin(\omega x+\phi)+K
$$

Where each of its elements are the following.

$$
f(x)=\underbrace{ A }_{\text{Amplitude}}\cdot \underbrace{ \sin }_{\text{Oscillator}}(\overbrace{ \omega }^{ \text{Period factor} } x+\underbrace{ \phi }_{\text{Phase} })+\overbrace{ K }^{ \text{Balance line} }
$$

It also has other components, such as the frequency and period, which are calculated with the following formulas.

$$
P=\frac{2\pi}{\omega}
$$
$$
f_{R}=\frac{1}{p}=\frac{\omega}{2\pi}
$$

>[!example]
>To get the period of $f\left(t\right)=\sqrt{17}\sin\left(\pi\left(\frac{t}{8}-\frac{\sqrt{2}}{11}\right)\right)-10$, we do the following:
>- We know that $P=\frac{2\pi}{\omega}$. First, we need to find $\omega$.
>- We distribute $\pi$ to get $\frac{\pi t}{8}-\frac{\sqrt{ 12 }}{11}$.
>- We factor out the coefficient of $t$ to find $\omega$.
>	- $\frac{\pi}{8}\left( t-\frac{8\sqrt{ 2 }}{11} \right)$.
>- Now, we can use the formula.
>	- $P=\frac{2\pi}{\frac{\pi}{8}}$
>	- $P=\frac{2\pi}{1}\div \frac{\pi}{8}$
>	- $P=16$
>
>Therefore, the period in this function is 16.

When graphed, a sine function looks like this.

![[sine wave.png|center|500]]

>[!note]
>The difference between a sine and [[Cosine|cosine]] wave is that the cosine wave starts at the crest, and the sine wave at the balance line. You could say that a cosine wave is just a sine wave with [[Horizontal displacement|horizontal displacement]]!

## Domain & range

- The [[Domain|domain]] for a sine function is always $(-\infty,\infty)$.
- The [[Range (functions)|range]] for a sine function is always $[K-A,K+A]$.

# Equation solutions

A [[Trigonometric equations|trigonometric equation]] with sine has infinite solutions. A general form can be stated as:

$$
x=s+2\pi k
$$

Where $s$ is the solution, and $k$ can be any number, since $2\pi$ is the period of the sine function, and there can be a solution for any number of values you put in $k$.