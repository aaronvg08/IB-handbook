AND is a [[Boolean operators|boolean operator]] that can be represented with the multiplication symbol ($*\;\text{or}\;\times$). It only returns true (1) if both values are also true. The truth table and symbols for AND can be found below.

![[AND.png|center]]