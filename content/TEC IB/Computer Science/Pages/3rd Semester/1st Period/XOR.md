XOR is a [[Boolean operators|boolean operator]] that is a combination of [[AND]] and [[OR]]. It returns true (1) when the values are different. The truth table and symbols for XOR are found below.

![[XOR.png|center]]

>[!note]
>Since we decrypt and encrypt with XOR, it means that XOR is an operation that reverses itself!