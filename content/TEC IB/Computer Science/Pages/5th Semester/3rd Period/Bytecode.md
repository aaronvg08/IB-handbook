A bytecode is an object created by bytecode [[Interpreter|interpreters]] or Just In Time (JIT) compilation that saves a piece of code from a program. This bytecode is then not directly run in the system, but rather on a virtual machine or an interpreter that understands the bytecode's instructions.

>[!error]
>Bytecode interpreters compile source code into bytecode before execution, so there could be errors in the runtime.