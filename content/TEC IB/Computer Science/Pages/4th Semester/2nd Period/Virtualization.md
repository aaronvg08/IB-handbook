Virtualization is something that happens when the [[Operating system (OS)|OS]] doesn't have enough RAM to open or load something. Through virtualization, you can use the secondary memory to represent a program instead of regular memory.

> [!danger] 
> This makes it so, so, so, so, so slow. You must avoid doing this, at all times.