The Arithmetic Logic Unit (ALU) is a component of the [[Central Processing Unit (CPU)]]. Its main purpose is to perform arithmetic and logical operations. It is where the actual computation of a computer happens. Operations can include:

- Addition
- Subtraction
- Logical operators (AND, OR, NOT, etc.)